
This is one way to setup an all in one(single node) openstack environment.
1. Begin by running the install_part1.sh script
2. Then manually setup your host networking getting hints referring install_manual.txt
3. Finally execute the install_part2.sh
4. Setup neutron networks for VMs by executing the openstack_test.sh

